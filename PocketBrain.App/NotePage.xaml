<phone:PhoneApplicationPage
    x:Class="PocketBrain.App.NotePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converter="clr-namespace:PhoneKit.Framework.Conversion;assembly=PhoneKit.Framework"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <toolkit:TransitionService.NavigationInTransition>
        <toolkit:NavigationInTransition>
            <toolkit:NavigationInTransition.Backward>
                <toolkit:SlideTransition Mode="SlideLeftFadeIn"/>
            </toolkit:NavigationInTransition.Backward>
            <toolkit:NavigationInTransition.Forward>
                <toolkit:SlideTransition Mode="SlideLeftFadeIn"/>
            </toolkit:NavigationInTransition.Forward>
        </toolkit:NavigationInTransition>
    </toolkit:TransitionService.NavigationInTransition>
    <toolkit:TransitionService.NavigationOutTransition>
        <toolkit:NavigationOutTransition>
            <toolkit:NavigationOutTransition.Backward>
                <toolkit:SlideTransition Mode="SlideRightFadeOut"/>
            </toolkit:NavigationOutTransition.Backward>
            <toolkit:NavigationOutTransition.Forward>
                <toolkit:SlideTransition Mode="SlideRightFadeOut"/>
            </toolkit:NavigationOutTransition.Forward>
        </toolkit:NavigationOutTransition>
    </toolkit:TransitionService.NavigationOutTransition>
    
    <phone:PhoneApplicationPage.Resources>
        <converter:BooleanToVisibilityConverter x:Key="boolToVis" />
        <converter:ShortDateConverter x:Key="shortDate" />
        <converter:ShortTimeConverter x:Key="shortTime" />
    </phone:PhoneApplicationPage.Resources>

    <!--LayoutRoot ist das Stammraster, in dem alle anderen Seiteninhalte platziert werden-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel enthält den Namen der Anwendung und den Seitentitel-->

        <!--ContentPanel - zusätzliche Inhalte hier platzieren-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <ScrollViewer>
                <StackPanel>
                    <toolkit:PhoneTextBox Hint="Untitled" Text="{Binding Title, Mode=TwoWay}" />
                    <Grid>
                        <TextBlock Text="{Binding DateCreated, Converter={StaticResource shortTime}}" Style="{StaticResource PhoneTextSmallStyle}" HorizontalAlignment="Left" />
                        <TextBlock Text="{Binding DateCreated, Converter={StaticResource shortDate}}" Style="{StaticResource PhoneTextSmallStyle}" HorizontalAlignment="Right" />
                    </Grid>
                    <TextBox x:Name="Text" Text="{Binding Content, Mode=TwoWay}"
                             Height="Auto" MinHeight="320" TextWrapping="Wrap" VerticalScrollBarVisibility="Visible" AcceptsReturn="True" />
                    <Grid Margin="12,0,12,0" Height="432" Visibility="{Binding HasAttachement, Converter={StaticResource boolToVis}}">
                        <Grid.Clip>
                            <RectangleGeometry Rect="0,0,432,432" />
                        </Grid.Clip>
                        <Image x:Name="AttachementImage"
                               Stretch="UniformToFill" Height="432"
                               ManipulationDelta="AttachementImage_ManipulationDelta"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center">
                            <Image.RenderTransform>
                                <CompositeTransform CenterX="216" CenterY="216" />
                            </Image.RenderTransform>
                        </Image>
                    </Grid>
                    <Button x:Name="ImageFromAblumButton" Content="Add attachement" Command="{Binding AddAttachementCommand}"
                        Visibility="{Binding HasAttachement, Converter={StaticResource boolToVis}, ConverterParameter=!}"/>
                    <Button x:Name="RemoveAttachementButton" Content="Remove attachement" Command="{Binding RemoveAttachementCommand}"
                        Visibility="{Binding HasAttachement, Converter={StaticResource boolToVis}}"/>
                    <Button  Content="pin" Command="{Binding PinToStartCommand}" />
                    <Button x:Name="DeleteNoteButton" Content="delelte/cancel" Command="{Binding DeleteCommand}" />
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>

</phone:PhoneApplicationPage>
    